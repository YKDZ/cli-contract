{
  "name": "@cli-contract/example",
  "private": true,
  "type": "module",
  "imports": {
    "#/*": {
      "types": "./src/*.ts",
      "default": "./dist/*.js"
    }
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json --pretty false",
    "format:check": "oxfmt --list-different --config ../../oxfmt.config.ts .",
    "format:write": "oxfmt --write --config ../../oxfmt.config.ts .",
    "lint": "oxlint --quiet --format=unix --config ../../oxlint.config.ts --ignore-pattern node_modules .",
    "lint:fix": "oxlint --format=unix --config ../../oxlint.config.ts . --fix",
    "start": "node src/main.ts",
    "test": "node --test test/*.test.ts",
    "test:e2e": "node --test test/*.e2e.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@ykdz/cli-contract": "workspace:*",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cli-contract/typescript-config": "workspace:*",
    "@types/node": "catalog:",
    "@ykdz/cli-contract-testing": "workspace:*",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript-7": "catalog:"
  },
  "engines": {
    "node": "24"
  }
}
